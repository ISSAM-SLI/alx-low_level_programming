#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: first int to be evaluated
 * @b: second int to be evaluated
 * Return: the multiplication of two integers
 */

int mul(int a, int b)
{
	return (a * b);
}
